# Garmin Connect credentials — copy this file to .env and fill in your values.
# NEVER commit .env to version control (it is gitignored).

GARMIN_EMAIL=your_email@example.com
GARMIN_PASSWORD=your_garmin_password
